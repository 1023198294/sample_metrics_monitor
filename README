
example of building a prometheus metrics monitor
monitor the usage of CPU and memory by implementing gopsutil in metrics form
it can be represented in prometheus UI by searching the metrics"request_resource"

